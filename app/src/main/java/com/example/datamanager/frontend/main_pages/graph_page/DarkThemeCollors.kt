// Documentation partially generated
// Refactoring done with copilot
// Some functionality implemented with use of copilot
// @ author: Michal Poprac
package com.example.datamanager.frontend.main_pages.graph_page

import androidx.compose.ui.graphics.Color

/**
 * Object containing the color definitions for the dark theme of the application.
 * These colors are used throughout the app to maintain a consistent dark theme design.
 */
object DarkThemeColors {
    /** Background color for the app's dark theme. */
    val background = Color(0xFF121212)

    /** Surface color for UI elements like cards and containers in the dark theme. */
    val surface = Color(0xFF1E1E1E)

    /** Variant of the surface color for slightly different UI elements. */
    val surfaceVariant = Color(0xFF2D2D2D)

    /** Primary color used for highlights and prominent UI elements. */
    val primary = Color(0xFF2196F3)

    /** Secondary color used for accents and less prominent UI elements. */
    val secondary = Color(0xFF03DAC6)

    /** Orange color used for accents and less prominent UI elements. */
    val orange = Color(0xFFFF9800)

    /** Color used for text and icons displayed on the background. */
    val onBackground = Color.White

    /** Color used for text and icons displayed on surfaces. */
    val onSurface = Color.White

    /** Color used to indicate errors or warnings in the UI. */
    val error = Color(0xFFCF6679)
}
